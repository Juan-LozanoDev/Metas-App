import React from "react";
import Credentials from "../../shared/Credentials";

function Register() {

    const send = async (form) => {
    }

  return (
    <Credentials send={send} title="Registro" button="Registrame"> </Credentials>
  )
}

export default Register;
